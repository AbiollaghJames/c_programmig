#include <stdio.h>

int main()
{
    int n;

    printf("size of type 'char': %lu\n", sizeof(char));
    printf("size of type 'int': %lu\n", sizeof(int));
    printf("size of type 'float': %lu\n", sizeof(float));
    printf("size of type 'n': %lu\n", sizeof(n));

    return (0);
}